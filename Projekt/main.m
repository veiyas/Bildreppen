%% Main

%Load file and adjust if neccessary
im = imread('test_images\example_image.jpg');
im = adjustInput(im);

%Calculate mean L,A,B values for each cell (32x16) in inputImage
% prot = extractCellColors(im); Work in progress



